// Global MSW Handlers array
// You can co-locate handlers in your feature folders and import them here
export const handlers = [
  // Example:
  // http.get('/api/example', () => HttpResponse.json({ data: 'mocked' }))
];
